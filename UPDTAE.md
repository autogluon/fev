We have cloned the fev-bench repo into our own repo. In case we want to get the latest updates from fev-bench, execute the functions below:
```bash
cd fev
git pull origin main
cd ..
git add fev
git commit -m "Update fev submodule"
```